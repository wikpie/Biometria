package com.example.biometria.Recognition;

import java.util.Map;

public class RecognitionTest {

    public void recognizeTest() {
        /*
        DataProcessor processor = new DataProcessor();
        Map<Integer, float[]> samples = processor.processData(***BYTE ARRAY***);

        Prediction prediction = new Prediction();
        prediction.fetchInterpreter(***CONTEXT***);
        Integer bestResult = prediction.predict(samples);

        String label = Labels.values()[bestResult].name();
        */
    }
}
